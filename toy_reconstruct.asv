function [ im_out ] = toy_reconstruct( toyim )
%TOY_RECONSTRUCT 

[height, width] = size(toyim);
pixel_count = height*width;
im2var = zeros(height, width);
im2var(1:pixel_count) = 1:pixel_count; 

% m equations, n variables
% 3*height*width equations = 3 equations for each pixel
A = sparse([], [], [], 3*pixel_count, pixel_count);
e = 1; % equation counter

for i = 1:3*pixel_count
    A(e, 
    e = e+1;
end


end

